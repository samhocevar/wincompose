<?xml version="1.0"?>
<!--
   - This file is used at runtime only. It defines how the application
   - should behave. It’s read-only.
  -->
<configuration>
  <startup>
    <!-- Instructs to run on .NET 4.x preferably. -->
    <supportedRuntime version="v4.0"/>
  </startup>
</configuration>
